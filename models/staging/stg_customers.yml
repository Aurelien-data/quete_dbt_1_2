# le fichier stg_xxx.yml sert à documenter les colonnes de la table que nous créons
# nous intervenons sur ce fichier pour une table en particulier

version: 2

models:
    - name: stg_customers
      columns:
        - name: customer_id  # nous de la colonne qui apparaitra dans la table
        - name: customer_name  # nous de la colonne qui apparaitra dans la table